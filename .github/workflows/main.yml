name: Run Mineflayer Bot

on:
  workflow_dispatch:   # chạy thủ công từ tab Actions

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 360   # tối đa 6 giờ
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Start bot
        env:
          SERVER_HOST: per10.asaka.asia   # 👈 có thể đổi tuỳ server
          SERVER_PORT: 30060
          BOT_COUNT: 30
          JOIN_INTERVAL_MS: 15000
          AUTH_MODE: offline
          BOT_PASS: 12345678@
        run: node multi_bot_fleet_with_surnames_final.js
